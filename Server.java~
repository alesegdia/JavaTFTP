import java.net.*;
import java.io.*;

public class Server {
    private DatagramSocket datagrama = null;

    public Server (int port) throws SocketException {
	datagram = new DatagramSocket(port);
    }

    public void sendPacket(byte[] data) throws IOException {
	DatagramSocket sendData = new DatagramPacket(data, data.length,
						     ;
	

	
